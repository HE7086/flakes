{
  services.home-assistant.config = {
    # default_config = { };
    # assist_pipeline = { };
    # bluetooth = { };
    config = { };
    cloud = { };
    # conversation = { };
    dhcp = { };
    # energy = { };
    history = { };
    homeassistant_alerts = { };
    logbook = { };
    # media_source = { };
    mobile_app = { };
    my = { };
    # ssdp = { };
    # stream = { };
    # sun = { };
    usb = { };
    webhook = { };
    zeroconf = { };

    input_button = { };
  };

}
